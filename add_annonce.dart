import 'package:flutter/material.dart';

class AddAnnonce extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(25.0),
      child: Text('Ajouter une annonce', style: TextStyle(fontSize: 36.0)),
    );
  }
}